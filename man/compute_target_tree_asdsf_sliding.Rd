% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_diagnostics.R
\name{compute_target_tree_asdsf_sliding}
\alias{compute_target_tree_asdsf_sliding}
\title{Sliding-window ASDSF for a target tree}
\usage{
compute_target_tree_asdsf_sliding(
  phy_target,
  edge_list_chains,
  window_size = 50L,
  burnin = 0L,
  rooted = TRUE,
  normalize = TRUE,
  min_freq = 0,
  ncores = 1,
  min_chains = 1L
)
}
\arguments{
\item{phy_target}{A rooted \code{phylo} object defining the reference clades.}

\item{edge_list_chains}{List of chains, each a list of edge matrices (2-column
integer matrices) ordered by iteration.}

\item{window_size}{Positive integer; number of successive samples in the base
window. Cumulative estimates are computed at multiples of this size.}

\item{burnin}{Non-negative integer; number of initial samples to discard from
each chain before windowing.}

\item{rooted}{Logical; treat trees as rooted when matching clades (passed to
\code{compute_target_tree_asdsf}).}

\item{normalize}{Logical; passed to \code{compute_target_tree_asdsf}.}

\item{min_freq}{Minimum clade frequency threshold when averaging SDs. Passed
to \code{compute_target_tree_asdsf}.}

\item{min_chains}{Minimum number of chains required after filtering.}
}
\value{
A data.frame with one row per window containing the ASDSF value, the
window bounds (1-indexed, post-burnin), and attributes \code{window_size}, \code{step},
and \code{burnin}.
}
\description{
Applies \code{compute_target_tree_asdsf()} to sequential, fixed-width windows of
each MCMC chain and returns an ASDSF trajectory where each point uses all
samples up to the end of the corresponding window (cumulative windows, akin to
\code{get_asdsfs}).
}
\seealso{
\link{compute_target_tree_asdsf}
}
