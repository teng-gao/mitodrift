# Convert a long format mutation data frame to a matrix

Convert a long format mutation data frame to a matrix

## Usage

``` r
long_to_mat(mut_dat_long, variable)
```

## Arguments

- mut_dat_long:

  A data frame with columns: variant, cell, variable

- variable:

  The variable to convert to a matrix

## Value

A matrix of values
